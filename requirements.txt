# Minimal requirements for WebSocket-only Django project
# This project ONLY needs WebSocket support, no database, no REST framework

Django>=4.2,<5.0
channels>=4.0.0
channels-redis>=4.1.0
daphne>=4.0.0
paho-mqtt>=1.6.1
djangorestframework==3.15.0
dj-database-url==2.1.0
psycopg2-binary==2.9.9
whitenoise==6.6.0
django-cors-headers>=4.3.0

firebase-admin==6.6.0
